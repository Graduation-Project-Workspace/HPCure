<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DemoApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.DemoApp" parent="Base.Theme.DemoApp" />

    <style name="Theme.DemoApp.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Background color -->
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="windowSplashScreenBackground">@color/splash_background</item>

        <!-- Animated splash icon -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>

        <!-- Duration of the icon animation -->
        <item name="windowSplashScreenAnimationDuration">1000</item>

        <!-- Post splash screen theme -->
        <item name="postSplashScreenTheme">@style/Theme.DemoApp</item>
    </style>


</resources>
